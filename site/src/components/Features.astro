---
---
<div class="grid grid-cols-1 sm:grid-cols-2 gap-4 my-12">
  <div class="card bg-base-200">
    <div class="card-body p-5">
      <h3 class="font-semibold text-sm">Single Binary</h3>
      <p class="text-xs text-base-content/50">Compiles to ~60MB standalone executable. No Node.js, no Bun, no Python needed at runtime.</p>
    </div>
  </div>
  <div class="card bg-base-200">
    <div class="card-body p-5">
      <h3 class="font-semibold text-sm">Web Dashboard</h3>
      <p class="text-xs text-base-content/50">Built-in UI at localhost:3713. Enable/disable tools, configure API keys, monitor invocations live.</p>
    </div>
  </div>
  <div class="card bg-base-200">
    <div class="card-body p-5">
      <h3 class="font-semibold text-sm">Auto-registers</h3>
      <p class="text-xs text-base-content/50">One command sets up both Claude Desktop and Claude Code. No manual JSON editing.</p>
    </div>
  </div>
  <div class="card bg-base-200">
    <div class="card-body p-5">
      <h3 class="font-semibold text-sm">Auto-Update</h3>
      <p class="text-xs text-base-content/50">Run <code class="bg-base-300 px-1.5 py-0.5 rounded text-xs">mcpy update</code> or use the mcpy_update tool to stay current.</p>
    </div>
  </div>
</div>
